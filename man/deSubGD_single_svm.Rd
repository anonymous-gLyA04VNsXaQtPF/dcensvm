% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{deSubGD_single_svm}
\alias{deSubGD_single_svm}
\title{Implements decentralized subgradient descent (D-subGD) for SVM with fixed regularization parameter.}
\usage{
deSubGD_single_svm(
  X,
  y,
  adjacency_matrix,
  B_init,
  T = 20L,
  a = 3.5,
  b = 0.51,
  lambda = 0.01,
  eps = 0.01,
  quiet = TRUE
)
}
\arguments{
\item{X}{Design matrix of size N × p (N total samples, p features).}

\item{y}{Response vector of length N (values should be +1 or -1).}

\item{adjacency_matrix}{Matrix defining communication links between nodes (m × m).}

\item{B_init}{Initial coefficient matrix of size p × m (one coefficient vector per node).}

\item{T}{Maximum number of iterations.}

\item{a}{Stepsize scaling parameter (default=3.5).}

\item{b}{Stepsize decay exponent (default=0.51).}

\item{lambda}{L1 regularization parameter (default=1e-2).}

\item{eps}{Convergence threshold for relative Frobenius norm change (default=1e-2).}

\item{quiet}{If true, suppresses console output (default=true).}
}
\value{
A list containing:
  - `B`: Final estimated coefficient matrix (p × m)
  - `history`: List containing:
       - `errors`: Vector of relative RMSE at each iteration.
}
\description{
This function performs decentralized training across multiple nodes defined by an adjacency matrix.
It uses a subgradient method with decaying step size (eta = a/(t+1)^b) and L1 regularization.
The algorithm converges when relative RMSE change falls below threshold.
}
