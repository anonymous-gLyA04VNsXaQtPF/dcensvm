% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{SubGD_hinge}
\alias{SubGD_hinge}
\title{Implements SVM training with BIC-regularized parameter selection for hinge loss.}
\usage{
SubGD_hinge(
  X,
  y,
  b_init,
  T = 20L,
  a_lambda = 3.5,
  b_lambda = 0.51,
  nlambda = 100L,
  lambda_factor = 1e-04,
  lambda_max = 1,
  eps = 1e-04,
  quiet = TRUE
)
}
\arguments{
\item{X}{Design matrix of size N × p (N samples, p features).}

\item{y}{Response vector of length N (values +1 or -1).}

\item{b_init}{Initial coefficient vector of length p.}

\item{T}{Maximum number of iterations per lambda.}

\item{a_lambda}{Step size scaling parameter (default=3.5).}

\item{b_lambda}{Step size decay exponent (default=0.51).}

\item{nlambda}{Number of lambda values in regularization path (default=100).}

\item{lambda_factor}{Maximum lambda in the regularization path before multiplied scale.}

\item{lambda_max}{Multiplied scale of all lambda in the regularization path.}

\item{eps}{Convergence threshold for relative coefficient change (default=1e-4).}

\item{quiet}{If true, suppresses console output (default=true).}
}
\value{
A list containing (for optimal lambda):
  - `b`: Final estimated coefficient vector (length p)
  - `history`: List containing:
       - `errors`: Vector of relative coefficient changes at each iteration
}
\description{
This function selects optimal lambda via BIC criterion from a regularization path.
Lambda values are logarithmically spaced from lambda_max to lambda_max*lambda_factor.
For each lambda, trains a model using SubGD_single_hinge and computes BIC as:
BIC = hinge_loss + log(N)*sqrt(log(N))*shat, where shat is number of non-zero coefficients.
Final model uses lambda minimizing BIC.
}
