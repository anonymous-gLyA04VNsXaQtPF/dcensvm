% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{decentralizedsvm_cpp}
\alias{decentralizedsvm_cpp}
\title{Implements a decentralized SVM algorithm using ADMM optimization.}
\usage{
decentralizedsvm_cpp(
  X,
  y,
  adjacency_matrix,
  B_init,
  betaT,
  h,
  type,
  C_N,
  T_outer = 10L,
  c0 = 0.1,
  tau_penalty_factor = 1/6,
  nlambda = 100L,
  lambda_factor = 1e-04,
  lambda_max = 1,
  lambda_0 = 0.5,
  C_rho = 1.01,
  quiet = TRUE,
  eps = 0.01
)
}
\arguments{
\item{X}{Design matrix of size N × p.}

\item{y}{Response vector of length N.}

\item{adjacency_matrix}{Matrix defining communication links between nodes.}

\item{B_init}{Initial coefficient matrix.}

\item{betaT}{True coefficient vector used for error calculation (ground truth).}

\item{h}{Kernel bandwidth.}

\item{type}{Kernel type (1–5).}

\item{C_N}{Regularization constant for BIC.}

\item{T_outer}{Number of iterations.}

\item{c0}{Constant for step-size control (not used here but kept for consistency).}

\item{tau_penalty_factor}{A penalty parameter in the augmented Lagrangian.}

\item{nlambda}{Number of regularization path points.}

\item{lambda_factor}{Maximum lambda in the regularization path before multiplied scale.}

\item{lambda_max}{Multiplied scale of all lambda in the regularization path.}

\item{lambda_0}{Regularization term of L2 penalty.}

\item{C_rho}{Scaling factor for rho_l update.}

\item{quiet}{Whether to suppress output messages.}

\item{eps}{Convergence tolerance (not currently used in stopping criterion).}
}
\value{
A list containing:
- `B`: Final estimated coefficients (p × m matrix).
- `history`: List containing:
  - `errors_outer`: Frobenius norm errors over iterations.
  - `loss_error`: Hinge loss over iterations.
  - `shat_array`: Estimated sparsity (number of non-zero coefficients) over iterations.
  - `accuracy`: Classification accuracy over iterations.
 - `bic_mat`: BIC under different lambda.
}
\description{
Supports various kernel types and performs feature selection using soft thresholding.
Lambda values are logarithmically spaced from 1 to lambda_max and then multiplied lambda_factor.
Also calculates accuracy, BIC, and other performance metrics.
}
