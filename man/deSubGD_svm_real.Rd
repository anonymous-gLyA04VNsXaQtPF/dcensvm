% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{deSubGD_svm_real}
\alias{deSubGD_svm_real}
\title{Implements decentralized subgradient descent (D-subGD) for SVM with BIC-regularized parameter selection in real-data analysis.}
\usage{
deSubGD_svm_real(
  X,
  y,
  adjacency_matrix,
  B_init,
  divisions,
  T = 20L,
  a = 3.5,
  b = 0.51,
  nlambda = 100L,
  lambda_factor = 1e-04,
  lambda_max = 1,
  eps = 0.01,
  quiet = TRUE
)
}
\arguments{
\item{X}{Design matrix of size N × p (N total samples, p features).}

\item{y}{Response vector of length N (values should be +1 or -1).}

\item{adjacency_matrix}{Matrix defining communication links between nodes (m × m).}

\item{B_init}{Initial coefficient matrix of size p × m (one coefficient vector per node).}

\item{divisions}{Vector indicating sample assignments to nodes (length N, values 1 to m).}

\item{T}{Maximum number of iterations.}

\item{a}{Step size scaling parameter.}

\item{b}{Step size decay exponent.}

\item{nlambda}{Number of lambda values in regularization path (default=100).}

\item{lambda_factor}{Maximum lambda in the regularization path before multiplied scale.}

\item{lambda_max}{Multiplied scale of all lambda in the regularization path.}

\item{eps}{Convergence threshold for relative Frobenius norm change (default=1e-2).}

\item{quiet}{If true, suppresses console output (default=true).}
}
\value{
A list containing (for optimal lambda):
  - `B`: Final estimated coefficient matrix (p × m)
  - `history`: List containing:
       - `errors`: Vector of relative RMSE at each iteration
}
\description{
This function selects optimal lambda via BIC criterion from a regularization path.
Lambda values are logarithmically spaced from 1 to lambda_max and then multiplied lambda_factor.
For each lambda, trains a model using deSubGD_single_svm_real and computes BIC.
Final model uses lambda minimizing BIC = hinge loss + log(N)*sqrt(log(N))*shat.
}
