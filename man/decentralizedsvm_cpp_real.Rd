% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{decentralizedsvm_cpp_real}
\alias{decentralizedsvm_cpp_real}
\title{Implements a decentralized penalized convoluted support vector machine (deCSVM) for real data analysis using ADMM optimization.}
\usage{
decentralizedsvm_cpp_real(
  X,
  y,
  divisions,
  adjacency_matrix,
  B_init,
  betaT,
  h,
  type,
  C_N,
  T_outer = 10L,
  c0 = 0.1,
  tau_penalty_factor = 1/6,
  nlambda = 100L,
  lambda_factor = 1e-04,
  lambda_max = 1,
  lambda_0 = 0.5,
  C_rho = 1.01,
  quiet = TRUE,
  eps = 0.01
)
}
\arguments{
\item{X}{Design matrix of size N × p (N total samples, p features).}

\item{y}{Response vector of length N.}

\item{divisions}{Real data divisions.}

\item{adjacency_matrix}{Matrix defining communication links between nodes (m × m).}

\item{B_init}{Initial coefficient matrix of size p × m (one coefficient vector per node).}

\item{betaT}{True coefficient vector used for error calculation (ground truth).}

\item{h}{Kernel bandwidth.}

\item{type}{Type of kernel function to use (1: Uniform, 2: Laplacian, 3: Logistic, 4: Gaussian, 5: Epanechnikov).}

\item{C_N}{Regularization constant used in BIC calculation.}

\item{T_outer}{Maximum number of iterations.}

\item{c0}{Constant for step-size control (not used here but kept for consistency).}

\item{tau_penalty_factor}{Penalty factor for consensus term based on spectral norm.}

\item{nlambda}{Number of regularization path points.}

\item{lambda_factor}{Maximum lambda in the regularization path before multiplied scale.}

\item{lambda_max}{Multiplied Scale of all lambda in the regularization path.}

\item{lambda_0}{Regularization term of L2 penalty.}

\item{C_rho}{Scaling factor for rho_l update.}

\item{quiet}{If true, suppresses console output.}

\item{eps}{Convergence tolerance (not currently used in stopping criterion).}

\item{lambda}{Fixed L1 regularization parameter.}
}
\value{
A list containing:
- `B`: Final estimated coefficients (p × m matrix).
}
\description{
This function performs decentralized training across multiple nodes defined by the adjacency matrix of real data.
It supports various kernel types for hinge loss computation and applies soft thresholding for feature selection.
}
